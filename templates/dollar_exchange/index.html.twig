{% extends 'base.html.twig' %}

{% block title %}Variación del Dólar por días/mes{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Variación del Dólar por días/mes</h1>
        
        {% if error %}
            <div class="alert alert-danger">
                <i class="bi bi-exclamation-triangle-fill"></i>
                {{ error }}
            </div>
        {% endif %}
        
        <form method="get" class="mb-4">
            <div class="row">
                <div class="col-md-4">
                    <div class="form-group">
                        <label for="year">Año a consultar</label>
                        <select name="year" id="year" class="form-control">
                            {% for y in range(2020, 2025) %}
                                <option value="{{ y }}" {{ y == year ? 'selected' : '' }}>{{ y }}</option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label for="month">Mes a consultar</label>
                        <select name="month" id="month" class="form-control">
                            {% for m in range(1, 12) %}
                                <option value="{{ "%02d"|format(m) }}" {{ m == month|number_format ? 'selected' : '' }}>
                                    {{ "%02d"|format(m) }}
                                </option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
                <div class="col-md-4 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-search"></i>
                        Buscar
                    </button>
                </div>
            </div>
        </form>

        {% include 'dollar_exchange/_table.html.twig' %}
    </div>
{% endblock %} 